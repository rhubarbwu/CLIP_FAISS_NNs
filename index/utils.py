def get_image_index_filename(dataset_name, directory, n_components):
    return "{}/{}_{}.index".format(directory, dataset_name, n_components)


def get_txt_index_filename(dataset_name, directory, n_components):
    return "{}/{}_{}.index".format(directory, dataset_name, n_components)
